buildscript {
    repositories {
        google()  // Ensure this is present
        mavenCentral()  // Ensure this is present
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.4.2'  // Your Android Gradle version
        classpath 'dev.flutter:flutter-gradle-plugin:1.0.0'  // <-- This is the problematic line
    }
}
